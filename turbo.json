{
  "$schema": "./node_modules/turbo/schema.json",
  "ui": "stream",
  "tasks": {
    "dev:generate-compile_commands": {
      "inputs": ["binding.gyp"],
      "outputs": ["compile_commands.json"]
    }
  }
}
