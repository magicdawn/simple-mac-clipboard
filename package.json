{
  "name": "simple-mac-clipboard",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-node": "node-gyp rebuild",
    "build-electron": "electron-rebuild --version `electron -v`",
    "test": "npm run build-electron && electron-mocha",
    "test-cover": "npm run build-node && nyc --reporter=lcov --reporter=text mocha",
    "report-cover": "codecov"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bindings": "^1.5.0",
    "nan": "^2.14.0",
    "prebuild-install": "^5.3.3"
  },
  "devDependencies": {
    "@magicdawn/eslint-config": "latest",
    "@magicdawn/prettier-config": "latest",
    "codecov": "^3.3.0",
    "electron": "^7.1.13",
    "electron-mocha": "^8.2.1",
    "electron-rebuild": "^1.10.0",
    "eslint": "^6.8.0",
    "husky": "latest",
    "lint-staged": "latest",
    "mocha": "^6.1.4",
    "nyc": "^14.0.0",
    "prebuild": "^10.0.0",
    "prettier": "latest",
    "should": "^13.2.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,less,vue}": [
      "prettier --write",
      "git add"
    ]
  }
}
